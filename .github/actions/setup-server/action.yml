name: setup-server
description: 安装 uv 并按锁文件同步 server 依赖。

runs:
  using: composite
  steps:
    # setup-uv 不提供浮动大版本标签。
    - uses: astral-sh/setup-uv@v10.0.1
    - run: uv sync --locked
      shell: bash
      working-directory: server
